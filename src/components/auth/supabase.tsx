import { supabase } from '@/lib/supabase'
import { Auth } from '@supabase/auth-ui-react'
import { ThemeSupa } from '@supabase/auth-ui-shared'

const SupabaseAuth = () => (
  <div className="w-full flex items-center justify-between space-x-5 rounded-[2.5rem] border-white border-2 bg-[#e0e0e038] px-8 mt-8"></div>
)

export default SupabaseAuth
